@model IEnumerable<AAOAdmin.Models.DriversAvailable>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DriversAvailableDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DriversAvailableDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.UserId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DriversAvailableId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DriversAvailableId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DriversAvailableId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
